# 实际直播间连接测试指南

本文档介绍如何连接到真实的 Bilibili 直播间进行测试。

## 前置准备

1. **Java 21** 已安装
2. **库已发布到本地 Maven**：
   ```bash
   ./gradlew publishToMavenLocal
   ```

## 快速测试示例

### 1. 创建测试项目

创建一个新的测试项目目录：

```bash
mkdir bili-live-test
cd bili-live-test
```

### 2. 创建 build.gradle

```gradle
plugins {
    id 'java'
    id 'application'
}

group = 'test'
version = '1.0'

repositories {
    mavenLocal()  // 使用本地 Maven 仓库
    mavenCentral()
}

dependencies {
    implementation 'cn.liqing:bili-live-danmu:4.0.0'
    implementation 'org.slf4j:slf4j-simple:2.0.16'  // 简单的日志实现
}

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

application {
    mainClass = 'test.LiveRoomTest'
}
```

### 3. 创建测试代码

创建 `src/main/java/test/LiveRoomTest.java`：

```java
package test;

import cn.liqing.bili.live.danmu.*;
import cn.liqing.bili.live.danmu.handler.*;
import cn.liqing.bili.live.danmu.model.*;

public class LiveRoomTest {
    public static void main(String[] args) throws Exception {
        // 1. 选择一个直播间（这里使用一个示例房间号，请替换为实际的直播间）
        long roomId = 3; // 请替换为你想测试的房间号
        
        System.out.println("正在连接到房间: " + roomId);
        
        // 2. 创建客户端
        DanmuClient client = new DanmuClient();
        
        // 3. 添加连接监听器
        client.setListener(new ConnectionListener() {
            @Override
            public void onOpen() {
                System.out.println("✅ WebSocket 连接已建立");
            }

            @Override
            public void onClose(int code, String reason, boolean remote) {
                System.out.println("❌ WebSocket 连接已关闭: " + reason);
            }

            @Override
            public void onError(Exception ex) {
                System.err.println("❌ 发生错误: " + ex.getMessage());
                ex.printStackTrace();
            }
        });
        
        // 4. 添加弹幕处理器
        client.addHandler(new DanmuHandler(danmu -> {
            System.out.println("💬 [弹幕] " + danmu.user.name + ": " + danmu.body);
            if (danmu.user.fansMedal != null) {
                System.out.println("    └─ 粉丝勋章: " + danmu.user.fansMedal.name 
                    + " Lv." + danmu.user.fansMedal.level);
            }
        }));
        
        // 5. 添加礼物处理器
        client.addHandler(new GiftHandler(gift -> {
            System.out.println("🎁 [礼物] " + gift.user.name + " 送出 " 
                + gift.name + " x" + gift.num);
            if (gift.price > 0) {
                System.out.println("    └─ 价值: " + gift.price + " 元");
            }
        }));
        
        // 6. 添加醒目留言处理器
        client.addHandler(new SuperChatHandler(sc -> {
            System.out.println("⭐ [SC] " + sc.user.name + " (" + sc.price + "元): " 
                + sc.body);
        }));
        
        // 7. 添加舰长处理器
        client.addHandler(new GuardHandler(guard -> {
            System.out.println("🛡️ [舰长] " + guard.user.name + " 购买了 " 
                + guard.name + " (价值 " + guard.price + " 元)");
        }));
        
        // 8. 添加互动处理器（进房等）
        client.addHandler(new InteractiveHandler(interactive -> {
            String typeStr = switch (interactive.type) {
                case 1 -> "进入直播间";
                case 2 -> "关注";
                case 3 -> "分享";
                default -> "互动";
            };
            System.out.println("👋 [" + typeStr + "] " + interactive.user.name);
        }));
        
        // 9. 连接到直播间（无需登录）
        System.out.println("正在创建认证信息...");
        Auth auth = Auth.create(roomId);
        
        System.out.println("开始连接...");
        client.connect(auth);
        
        // 10. 保持运行 5 分钟
        System.out.println("已连接！监听中... (将运行 5 分钟)");
        System.out.println("按 Ctrl+C 可提前退出");
        Thread.sleep(5 * 60 * 1000);
        
        // 11. 断开连接
        System.out.println("\n正在断开连接...");
        client.disconnect();
        System.out.println("测试完成！");
    }
}
```

### 4. 运行测试

```bash
./gradlew run
```

## 高级测试场景

### 测试 1: 测试不同房间

修改 `roomId` 变量测试不同的直播间：

```java
long roomId = 3;      // 官方直播间
// long roomId = 12345;  // 其他直播间
```

### 测试 2: 测试带登录的功能（发送弹幕）

如果你想测试发送弹幕功能，需要提供 Cookie：

```java
// 从浏览器获取 Cookie
String cookie = "你的Cookie"; // 从浏览器开发者工具获取

// 创建带登录的认证
Auth auth = Auth.create(roomId, cookie);
client.connect(auth);

// 等待连接建立后发送弹幕
Thread.sleep(3000);
DanmuClient.send(cookie, String.valueOf(roomId), "测试弹幕");
```

### 测试 3: 只监听特定类型的消息

```java
// 只监听弹幕
client.addHandler(new DanmuHandler(danmu -> {
    System.out.println(danmu.user.name + ": " + danmu.body);
}));

// 不添加其他 handler
```

### 测试 4: 长时间稳定性测试

```java
// 运行 1 小时测试连接稳定性
Thread.sleep(60 * 60 * 1000);
```

## 获取 Cookie 的方法

### 方法 1: 从浏览器获取

1. 打开 https://live.bilibili.com
2. 登录你的账号
3. 按 F12 打开开发者工具
4. 切换到 "Network" (网络) 标签
5. 刷新页面
6. 点击任意请求
7. 在 "Headers" (请求头) 中找到 "Cookie"
8. 复制整个 Cookie 值

### 方法 2: 使用浏览器控制台

在浏览器控制台运行：

```javascript
document.cookie
```

复制输出的值。

## 测试检查清单

运行测试时，验证以下功能：

- [ ] WebSocket 成功连接
- [ ] 能接收到弹幕消息
- [ ] 能接收到礼物消息
- [ ] 能接收到醒目留言（如果直播间有）
- [ ] 能接收到舰长购买消息（如果直播间有）
- [ ] 能接收到用户进房消息
- [ ] 粉丝勋章信息正确显示
- [ ] 舰长等级正确显示
- [ ] 长时间运行无内存泄漏
- [ ] 断线重连功能正常（可以手动测试）

## 常见问题

### Q1: 连接失败

**可能原因**：
- 房间号不存在或输入错误
- 网络问题
- 房间未开播

**解决方法**：
- 检查房间号是否正确
- 尝试使用其他房间号
- 检查网络连接

### Q2: 收不到消息

**可能原因**：
- 直播间没有人发消息
- Handler 没有正确添加

**解决方法**：
- 选择一个热门直播间测试
- 检查 handler 是否正确添加

### Q3: 发送弹幕失败

**可能原因**：
- Cookie 过期或无效
- 没有发送弹幕权限（新账号需要绑定手机）
- 房间禁言

**解决方法**：
- 重新获取 Cookie
- 检查账号权限
- 换一个房间测试

## 性能监控

### 监控内存使用

```bash
# 运行时添加 JVM 参数
./gradlew run -Dorg.gradle.jvmargs="-Xmx256m -XX:+PrintGCDetails"
```

### 监控线程

可以添加代码查看线程情况：

```java
Thread.getAllStackTraces().keySet().forEach(t -> {
    System.out.println("Thread: " + t.getName() + " - " + t.getState());
});
```

## 示例输出

成功运行时应该看到类似输出：

```
正在连接到房间: 3
正在创建认证信息...
开始连接...
✅ WebSocket 连接已建立
已连接！监听中... (将运行 5 分钟)
按 Ctrl+C 可提前退出
💬 [弹幕] 用户A: 666
💬 [弹幕] 用户B: 主播加油
    └─ 粉丝勋章: 某主播 Lv.5
🎁 [礼物] 用户C 送出 辣条 x1
👋 [进入直播间] 用户D
⭐ [SC] 用户E (50元): 支持主播！
```

## 下一步

测试成功后，你可以：

1. 在你的项目中集成此库
2. 根据需要自定义消息处理逻辑
3. 添加数据存储、统计等功能
4. 部署到生产环境

## 支持

如果遇到问题：
- 查看详细日志输出
- 检查 [TESTING.md](TESTING.md) 了解单元测试
- 在 GitHub Issues 提问
