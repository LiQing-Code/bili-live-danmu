# 快速测试指南

## 1. 检查 Java 版本

```bash
java -version
```

需要 Java 21 或更高版本。

## 2. 运行所有测试

```bash
./gradlew test
```

## 3. 查看测试报告

测试完成后，用浏览器打开：

```
build/reports/tests/test/index.html
```

## 4. 本地构建

```bash
./gradlew clean build
```

成功后在 `build/libs/` 目录查看生成的 JAR 包。

## 5. 发布到本地 Maven

```bash
./gradlew publishToMavenLocal
```

这会把库安装到 `~/.m2/repository/cn/liqing/bili-live-danmu/4.0.0/`

## 6. 在其他项目中测试

在你的项目的 `build.gradle` 中添加：

```gradle
repositories {
    mavenLocal()  // 使用本地 Maven 仓库
    mavenCentral()
}

dependencies {
    implementation 'cn.liqing:bili-live-danmu:4.0.0'
}
```

## 测试覆盖

✅ **36 个单元测试全部通过**

- MessageTest (5 tests) - Gson 序列化/反序列化测试
- AuthTest (4 tests) - 认证功能测试
- BiliWbiSignTest (6 tests) - WBI 签名测试
- PacketTest (9 tests) - 数据包打包/解包测试
- DanmuHandlerTest (6 tests) - 弹幕处理测试
- GiftHandlerTest (6 tests) - 礼物处理测试

## 问题排查

### gradlew 没有执行权限

```bash
chmod +x gradlew
```

### 跳过测试进行构建

```bash
./gradlew build -x test
```

更多详细信息请查看 [TESTING.md](TESTING.md)
