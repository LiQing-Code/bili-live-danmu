# 哔哩哔哩直播间弹幕协议文档

## 概述

本文档描述了哔哩哔哩直播间弹幕的完整协议规范，包括连接、认证、心跳、消息收发等全流程。

## 连接信息

- **服务器地址**: `wss://broadcastlv.chat.bilibili.com:2245/sub`
- **协议**: WebSocket over TLS
- **编码**: UTF-8

---

## 数据包结构

### 包头格式（16 字节）

所有数据包都以固定的 16 字节包头开始：

```
偏移量(字节)  字段名           类型      说明
0-3          总长度           int32    包头+包体的总字节数（大端序）
4-5          包头长度         int16    固定为 16（大端序）
6-7          版本号           int16    0 = 未压缩, 2 = zlib 压缩（大端序）
8-11         操作码           int32    见下表（大端序）
12-15        序列号           int32    固定为 0（大端序）
16+          包体             bytes    可选，取决于版本号
```

### 操作码定义

| 操作码 | 名称 | 方向 | 说明 |
|--------|------|------|------|
| 2 | `HEARTBEAT` | 客户端→服务器 | 心跳包，30秒发送一次 |
| 3 | `HEARTBEAT_REPLY` | 服务器→客户端 | 心跳回复 |
| 5 | `SEND_SMS_REPLY` | 服务器→客户端 | 弹幕消息 |
| 7 | `AUTH` | 客户端→服务器 | 认证包（连接后首先发送） |
| 8 | `AUTH_REPLY` | 服务器→客户端 | 认证回复 |

### 版本号说明

- **版本 0**: 包体是实际的 JSON 数据
- **版本 2**: 包体使用 zlib 压缩，需要解压后重新解析

---

## 流程说明

### 1. WebSocket 连接建立
客户端创建 WebSocket 连接到服务器。

### 2. 发送认证包
客户端发送操作码为 7（AUTH）的数据包，包体为 JSON 格式的认证信息。

#### 认证 JSON 格式

```json
{
  "uid": 0,
  "roomid": 12345,
  "protover": 2,
  "platform": "web",
  "type": 2,
  "key": "xxxxxxxxxxxxx",
  "buvid": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `uid` | long | 用户 ID，匿名为 0 |
| `roomid` | long | 直播间 ID |
| `protover` | int | 协议版本，固定为 2 |
| `platform` | string | 平台，固定为 "web" |
| `type` | int | 客户端类型，固定为 2 |
| `key` | string | 认证令牌（需从 API 获取） |
| `buvid` | string | 设备 ID（任意 UUID 格式） |

#### 获取认证令牌

发送 HTTP GET 请求获取 `key` 值：

```
GET https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo?id={roomid}&type=0
```

需要进行 WBI 签名（详见下方签名算法部分），响应为 JSON：

```json
{
  "code": 0,
  "data": {
    "token": "xxxxxxxxxxxxx"
  }
}
```

### 3. 接收认证回复
服务器返回操作码为 8（AUTH_REPLY）的数据包。

### 4. 定期发送心跳
连接建立后，每 30 秒发送一个操作码为 2（HEARTBEAT）的数据包，包体为空。

### 5. 接收弹幕消息
服务器推送操作码为 5（SEND_SMS_REPLY）的数据包，包体为 JSON 格式的消息。

---

## 弹幕消息格式

### 消息外层结构

```json
{
  "cmd": "DANMU_MSG",
  "data": {
    "e": 0,
    "t": 1,
    "r": 1,
    "u": [12345, "用户名", 0, 0, 0, 10000, 1, ""],
    "p": [{"y": 1}],
    "m": "弹幕内容"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `cmd` | string | 命令类型 |
| `data` | object | 命令数据 |

### 主要命令类型

| cmd 值 | 说明 |
|--------|------|
| `DANMU_MSG` | 弹幕消息 |
| `SEND_GIFT` | 赠送礼物 |
| `GUARD_BUY` | 大航海购买 |
| `SUPER_CHAT_MESSAGE` | 醒目留言 |
| `INTERACT_WORD` | 交互事件（关注、进场等） |
| `LIVE_INTERACTIVE_GAME` | 直播互动游戏 |

### 弹幕消息详细数据

```
data.u[0]   - 用户 ID (int64)
data.u[1]   - 用户名 (string)
data.u[2]   - 是否是主播 (int, 0=否 1=是)
data.u[3]   - 用户等级 (int)
data.u[4]   - 用户勋章等级 (int)
data.u[5]   - 未知 (int)
data.u[6]   - 未知 (int)
data.u[7]   - 用户身份标识 (string)

data.m      - 弹幕文本内容 (string)
```

---

## 数据包编码/解码示例

### 打包流程（发送心跳）

```
1. 操作码: 2 (HEARTBEAT)
2. 包体: 空 (0 字节)
3. 包头构造:
   总长度 = 16 + 0 = 16 (0x00000010)
   包头长度 = 16 (0x0010)
   版本号 = 0 (0x0000)
   操作码 = 2 (0x00000002)
   序列号 = 0 (0x00000000)
4. 字节流 (16进制): 00 00 00 10 00 10 00 00 00 00 00 02 00 00 00 00
```

### 解包流程（接收消息）

```
1. 读取包头 (16 字节)
   - 字节 0-3: 总长度
   - 字节 6-7: 版本号
   - 字节 8-11: 操作码
2. 计算包体长度 = 总长度 - 16
3. 读取包体数据
4. 如果版本号为 2:
   - 对包体进行 zlib 解压
   - 递归解析解压后的数据
5. 如果版本号为 0:
   - 将包体作为 UTF-8 字符串解析
   - 解析为 JSON 对象
```

---

## WBI 签名算法

获取 `key` 时需要对请求进行 WBI 签名。该项目中签名由 `BiliWbiSign` 类处理。

签名步骤：

1. 从浏览器 Cookie 中提取 `wbi_b2_b3` 和 `wbi_b2_v` 的值
2. 对参数进行 MD5 签名
3. 将签名附加到请求 URL

> 完整的 WBI 签名算法较为复杂，建议参考 `BiliWbiSign.java` 实现

---

## 完整的连接示例

### 伪代码

```
// 1. 建立 WebSocket 连接
ws = WebSocket("wss://broadcastlv.chat.bilibili.com:2245/sub")
ws.onOpen = () => {
    // 2. 获取认证令牌
    token = HTTP_GET("https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo")
    
    // 3. 构造认证数据
    auth = {
        uid: 0,
        roomid: ROOM_ID,
        protover: 2,
        platform: "web",
        type: 2,
        key: token,
        buvid: GENERATE_UUID()
    }
    
    // 4. 发送认证包 (操作码 7)
    packet = createPacket(7, JSON.stringify(auth).getBytes())
    ws.send(packet.toByteBuffer())
    
    // 5. 启动心跳定时器 (30秒)
    setInterval(() => {
        heartbeatPacket = createPacket(2, [])
        ws.send(heartbeatPacket.toByteBuffer())
    }, 30000)
}

ws.onMessage = (bytes) => {
    // 6. 接收数据包
    packets = parsePackets(bytes)
    for packet in packets:
        if packet.operation == 5:  // SEND_SMS_REPLY
            message = JSON.parse(packet.body)
            handleMessage(message)
}

// 核心函数
createPacket(operation, body):
    length = 16 + body.length
    buffer = ByteBuffer(length)
    buffer.putInt(length)           // 总长度
    buffer.putShort(16)             // 包头长度
    buffer.putShort(0)              // 版本号
    buffer.putInt(operation)        // 操作码
    buffer.putInt(0)                // 序列号
    buffer.put(body)                // 包体
    return buffer

parsePackets(buffer):
    packets = []
    offset = 0
    while offset + 16 <= buffer.length:
        length = buffer.getInt(offset)
        version = buffer.getShort(offset + 6)
        operation = buffer.getInt(offset + 8)
        bodyLen = length - 16
        body = buffer.slice(offset + 16, bodyLen)
        
        if version == 2:  // 压缩包
            body = zlibDecompress(body)
            packets.addAll(parsePackets(body))
        else:  // 未压缩
            packet = new Packet(operation, body)
            packets.add(packet)
        
        offset += length
    
    return packets
```

---

## 其他语言实现建议

### Python 示例框架

```python
import websocket
import json
import struct
import zlib

class BiliDanmuClient:
    def __init__(self, room_id):
        self.room_id = room_id
        self.ws = websocket.WebSocketApp(
            "wss://broadcastlv.chat.bilibili.com:2245/sub",
            on_open=self.on_open,
            on_message=self.on_message,
            on_error=self.on_error
        )
    
    def on_open(self):
        # 获取 token 并认证
        auth_data = self.create_auth()
        self.send_packet(7, auth_data)
    
    def create_auth(self):
        # 实现认证逻辑
        return json.dumps({...}).encode('utf-8')
    
    def send_packet(self, operation, body):
        length = 16 + len(body)
        header = struct.pack('>IHHII', 
            length, 16, 0, operation, 0)
        self.ws.send(header + body, websocket.ABNF.OPCODE_BINARY)
    
    def on_message(self, data):
        packets = self.parse_packets(data)
        for packet in packets:
            if packet['operation'] == 5:
                message = json.loads(packet['body'].decode('utf-8'))
                self.handle_message(message)
    
    def parse_packets(self, data):
        packets = []
        offset = 0
        while offset + 16 <= len(data):
            length, header_len, version, op, seq = \
                struct.unpack('>IHHII', data[offset:offset+16])
            body_len = length - 16
            body = data[offset+16:offset+length]
            
            if version == 2:
                body = zlib.decompress(body)
                packets.extend(self.parse_packets(body))
            else:
                packets.append({
                    'operation': op,
                    'body': body
                })
            
            offset += length
        return packets
    
    def handle_message(self, message):
        cmd = message.get('cmd')
        # 处理不同类型的消息
        pass
```

### Node.js 示例框架

```javascript
const WebSocket = require('ws');
const zlib = require('zlib');

class BiliDanmuClient {
    constructor(roomId) {
        this.roomId = roomId;
        this.ws = new WebSocket('wss://broadcastlv.chat.bilibili.com:2245/sub');
        this.ws.on('open', () => this.onOpen());
        this.ws.on('message', (data) => this.onMessage(data));
    }
    
    async onOpen() {
        const authData = await this.createAuth();
        this.sendPacket(7, authData);
    }
    
    sendPacket(operation, body) {
        const length = 16 + body.length;
        const header = Buffer.allocUnsafe(16);
        header.writeUInt32BE(length, 0);
        header.writeUInt16BE(16, 4);
        header.writeUInt16BE(0, 6);
        header.writeUInt32BE(operation, 8);
        header.writeUInt32BE(0, 12);
        this.ws.send(Buffer.concat([header, body]));
    }
    
    onMessage(data) {
        const packets = this.parsePackets(data);
        packets.forEach(packet => {
            if (packet.operation === 5) {
                const message = JSON.parse(packet.body.toString('utf-8'));
                this.handleMessage(message);
            }
        });
    }
    
    parsePackets(data) {
        const packets = [];
        let offset = 0;
        
        while (offset + 16 <= data.length) {
            const length = data.readUInt32BE(offset);
            const version = data.readUInt16BE(offset + 6);
            const operation = data.readUInt32BE(offset + 8);
            const bodyLen = length - 16;
            let body = data.slice(offset + 16, offset + length);
            
            if (version === 2) {
                body = zlib.inflateSync(body);
                packets.push(...this.parsePackets(body));
            } else {
                packets.push({ operation, body });
            }
            
            offset += length;
        }
        
        return packets;
    }
    
    handleMessage(message) {
        const cmd = message.cmd;
        // 处理不同类型的消息
    }
}
```

---

## 常见问题

### Q: 为什么收到的数据是压缩的？
A: 服务器可能发送版本号为 2 的数据包，需要使用 zlib 解压缩后再处理。

### Q: 可以不发送心跳吗？
A: 不可以。如果不发送心跳，服务器会在 60 秒后断开连接。

### Q: 如何获取用户信息（头像、等级等）？
A: 这些信息通常包含在 `DANMU_MSG` 等消息的 `data` 字段中，需要解析相应的字段。

### Q: 是否可以发送弹幕？
A: 可以，但需要调用 Bilibili API，不是通过 WebSocket。参考 `DanmuClient.send()` 方法。

---

## 相关资源

- [项目仓库](https://github.com/LiQing-Code/bili-live-danmu)
- [Bilibili 直播 API 文档](https://github.com/SocialSisterYi/bilibili-API-collect)
- [zlib 压缩](https://zh.wikipedia.org/wiki/Zlib)

