# 哔哩哔哩直播间弹幕协议规范

## 基本信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | `wss://broadcastlv.chat.bilibili.com:2245/sub` |
| 协议 | WebSocket over TLS |
| 编码 | UTF-8 |

---

## 数据包格式

### 包头结构（固定 16 字节，大端序）

```
偏移  字段        类型    说明
0-3   总长度      int32   包头 + 包体的总字节数
4-5   包头长度    int16   固定值 16
6-7   版本号      int16   0=未压缩 | 2=zlib压缩
8-11  操作码      int32   见下表
12-15 序列号      int32   固定值 0
16+   包体        bytes   JSON 或压缩数据
```

### 操作码表

| 码值 | 名称 | 方向 | 描述 |
|-----|------|------|------|
| 2 | HEARTBEAT | C→S | 心跳包（每30秒发送一次） |
| 3 | HEARTBEAT_REPLY | S→C | 心跳回复 |
| 5 | SEND_SMS_REPLY | S→C | 弹幕消息 |
| 7 | AUTH | C→S | 认证包（连接后立即发送） |
| 8 | AUTH_REPLY | S→C | 认证成功确认 |

---

## 连接流程

### 1. 建立 WebSocket 连接
连接到 `wss://broadcastlv.chat.bilibili.com:2245/sub`

### 2. 发送认证包（操作码 7）
**包体内容**（JSON 格式，UTF-8 编码）：

```json
{
  "uid": 0,
  "roomid": 房间ID,
  "protover": 2,
  "platform": "web",
  "type": 2,
  "key": "认证令牌",
  "buvid": "设备ID"
}
```

**字段说明**：
- `uid`: 用户 ID，匿名时为 0
- `roomid`: 直播间 ID（必需）
- `protover`: 协议版本，固定为 2
- `platform`: 平台标识，固定为 "web"
- `type`: 客户端类型，固定为 2
- `key`: 认证令牌，通过 API 获取（见下方）
- `buvid`: 设备 ID，可为任意 UUID 格式

**获取认证令牌**：

```
GET https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo?id={roomid}&type=0
```

需进行 WBI 签名（参见 WBI 签名章节），返回 JSON：

```json
{
  "code": 0,
  "data": {
    "token": "xxxxx"
  }
}
```

提取 `data.token` 作为 `key` 值。

### 3. 接收认证回复
等待服务器发送操作码 8（AUTH_REPLY）的数据包，表示认证成功。

### 4. 启动心跳循环
每 30 秒发送一个操作码为 2（HEARTBEAT）的数据包，包体为空。

### 5. 接收消息
监听来自服务器的操作码 5（SEND_SMS_REPLY）数据包，解析 JSON 消息。

---

## WBI 签名算法

### 签名步骤

1. 准备参数 Map：`{"id": roomid, "type": "0"}`
2. 从浏览器 Cookie 中提取 `wbi_b2_b3` 和 `wbi_b2_v` 值（仅当登录时需要）
3. 对参数进行排序、拼接、MD5 哈希处理
4. 将签名附加到 URL

### 简化方案

对于匿名用户，可尝试直接附加参数（不签名）：

```
GET https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo?id={roomid}&type=0
```

如需完整 WBI 签名实现，参考 Bilibili 开源文档。

---

## 消息解析

### 消息结构

来自服务器的 JSON 消息顶层字段：

```json
{
  "cmd": "命令类型",
  "data": {
    // 具体数据，取决于 cmd
  },
  "info": []  // 可选的附加信息
}
```

### 主要命令类型

| cmd 值 | 说明 |
|--------|------|
| DANMU_MSG | 弹幕消息 |
| SEND_GIFT | 赠送礼物 |
| GUARD_BUY | 大航海购买 |
| SUPER_CHAT_MESSAGE | 醒目留言 |
| INTERACT_WORD | 交互事件（关注、进场等） |
| LIVE_INTERACTIVE_GAME | 互动游戏 |

### 弹幕消息（cmd="DANMU_MSG"）

```json
{
  "cmd": "DANMU_MSG",
  "data": {
    "e": 0,
    "t": 1,
    "r": 1,
    "u": [用户ID, "用户名", 是否主播, 用户等级, 勋章等级, 未知, 未知, "身份标识"],
    "p": [{"y": 位置}],
    "m": "弹幕内容"
  }
}
```

**主要字段**：
- `data.u[0]`: 用户 ID (int64)
- `data.u[1]`: 用户名 (string)
- `data.u[2]`: 是否主播 (int: 0=否, 1=是)
- `data.m`: 弹幕文本 (string)

---

## 数据包处理

### 打包（发送数据）

```
1. 准备包体 body（字节数组）
2. 计算总长度 = 16 + body.length
3. 构造包头（16字节，大端序）：
   [总长度(4), 16(2), 0(2), 操作码(4), 0(4)]
4. 拼接包头 + 包体，发送
```

**心跳包示例**（16进制）：
```
00 00 00 10  // 总长度 = 16
00 10        // 包头长度 = 16
00 00        // 版本号 = 0
00 00 00 02  // 操作码 = 2 (HEARTBEAT)
00 00 00 00  // 序列号 = 0
```

### 解包（接收数据）

```
1. 读取包头（16字节）
2. 解析总长度、版本号、操作码
3. 计算包体长度 = 总长度 - 16
4. 读取对应长度的包体
5. 如果版本号 = 2：
   a. 对包体进行 zlib 解压
   b. 将解压结果作为新的二进制流，重复步骤 1-4
6. 如果版本号 = 0：
   a. 将包体解码为 UTF-8 字符串
   b. 解析为 JSON 对象
7. 处理消息后，继续读下一个数据包（可能一次收到多个包）
```

---

## 多包处理

WebSocket 可能一次返回多个连续的数据包。处理流程：

```
接收的二进制流:
[包1头(16)] [包1体] [包2头(16)] [包2体] ...

解包伪代码:
offset = 0
while offset + 16 <= buffer.length:
    length = read_int32_be(buffer, offset)
    if offset + length > buffer.length:
        break  // 数据不完整
    version = read_int16_be(buffer, offset + 6)
    operation = read_int32_be(buffer, offset + 8)
    body = buffer[offset+16 : offset+length]
    
    if version == 2:
        body = zlib_decompress(body)
        // 递归处理解压后的数据
        parse_packets(body)
    else:
        json_obj = parse_json(body.decode('utf-8'))
        handle_message(json_obj)
    
    offset += length
```

---

## 认证示例

### 完整连接序列

```
1. 连接 WebSocket
   -> 连接成功

2. 获取认证令牌
   GET https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo?id=123&type=0
   <- { "data": { "token": "xxxxx" } }

3. 构造认证 JSON
   {
     "uid": 0,
     "roomid": 123,
     "protover": 2,
     "platform": "web",
     "type": 2,
     "key": "xxxxx",
     "buvid": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx11111infoc"
   }

4. 发送认证包（操作码 7）
   [包头] + [认证 JSON UTF-8 编码]
   ->

5. 接收认证回复（操作码 8）
   <-

6. 启动心跳（每30秒）
   [心跳包头] + []
   -> (重复)

7. 接收消息（操作码 5）
   <- [消息包头] + [消息 JSON]
```

---

## 常见情景

### 情景 1：收到压缩包（版本号=2）

```
收到: [包头(版本=2)] [压缩数据]
处理:
  1. 提取压缩数据
  2. zlib 解压
  3. 递归解析解压数据（可能包含多个数据包）
```

### 情景 2：连接断开

```
WebSocket 关闭时：
  1. 停止心跳定时器
  2. 重新连接
  3. 重新认证
```

### 情景 3：收到多个消息

```
一次收到多个数据包：
  [包1] [包2] [包3] ...
  
循环解析：
  for 包 in 所有包:
    if 包类型 == 5:  // SEND_SMS_REPLY
      处理消息(包体)
```

---

## 重要提示

1. **心跳必须**: 不发送心跳会导致服务器 60 秒后断开连接
2. **版本号判断**: 必须检查版本号，版本 2 的包需要解压
3. **多包处理**: 一次 WebSocket 消息可能包含多个数据包，需逐个解析
4. **认证有效期**: 认证令牌有效期有限，长连接需定期更新
5. **大端序**: 所有整数字段均使用大端序（网络字节序）
6. **UTF-8 编码**: 所有 JSON 和字符串使用 UTF-8 编码

